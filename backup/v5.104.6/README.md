# v5.104.6 pre-release backup

Created by `npm run release` before publishing v5.104.6.

- Pre-release commit: `3732eed`
- Backup tag: `backup/pre-v5.104.6`
- Release tag: `v5.104.6`
- Release message: 授權腳本可指定瀏覽器，避免預設瀏覽器與登入帳號不同時卡住
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.104.6`.
