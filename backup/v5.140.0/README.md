# v5.140.0 pre-release backup

Created by `npm run release` before publishing v5.140.0.

- Pre-release commit: `ea5cd4c`
- Backup tag: `backup/pre-v5.140.0`
- Release tag: `v5.140.0`
- Release message: feat: deploy FunnyTools Phase 2B controlled index experiment
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.140.0`.
