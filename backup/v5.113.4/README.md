# v5.113.4 pre-release backup

Created by `npm run release` before publishing v5.113.4.

- Pre-release commit: `355d15e`
- Backup tag: `backup/pre-v5.113.4`
- Release tag: `v5.113.4`
- Release message: fix: remove stale localized tool count
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.113.4`.
