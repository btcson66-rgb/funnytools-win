# v5.127.0 pre-release backup

Created by `npm run release` before publishing v5.127.0.

- Pre-release commit: `868261f`
- Backup tag: `backup/pre-v5.127.0`
- Release tag: `v5.127.0`
- Release message: publish task-013 international SEO locale quality gate
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.127.0`.
