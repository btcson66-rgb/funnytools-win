# v5.112.0 pre-release backup

Created by `npm run release` before publishing v5.112.0.

- Pre-release commit: `e4d4d7c`
- Backup tag: `backup/pre-v5.112.0`
- Release tag: `v5.112.0`
- Release message: feat: expand Shopee support catalog
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.112.0`.
