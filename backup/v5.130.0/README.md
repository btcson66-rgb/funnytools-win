# v5.130.0 pre-release backup

Created by `npm run release` before publishing v5.130.0.

- Pre-release commit: `ab4750f`
- Backup tag: `backup/pre-v5.130.0`
- Release tag: `v5.130.0`
- Release message: publish task-016 search performance lifecycle pipeline
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.130.0`.
