# v5.10.0 pre-release backup

Created by `npm run release` before publishing v5.10.0.

- Pre-release commit: `46b50b8`
- Backup tag: `backup/pre-v5.10.0`
- Release tag: `v5.10.0`
- Release message: feat: shopee affiliate slot system (dormant, zero output until enabled)
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.10.0`.
