# v5.25.0 pre-release backup

Created by `npm run release` before publishing v5.25.0.

- Pre-release commit: `9f84ec4`
- Backup tag: `backup/pre-v5.25.0`
- Release tag: `v5.25.0`
- Release message: feat: publish qr-code-classroom-guide from content library
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.25.0`.
