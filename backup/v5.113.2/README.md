# v5.113.2 pre-release backup

Created by `npm run release` before publishing v5.113.2.

- Pre-release commit: `826e3d9`
- Backup tag: `backup/pre-v5.113.2`
- Release tag: `v5.113.2`
- Release message: fix: audit affiliate metadata and disclosure copy
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.113.2`.
