# v5.104.2 pre-release backup

Created by `npm run release` before publishing v5.104.2.

- Pre-release commit: `46f3e93`
- Backup tag: `backup/pre-v5.104.2`
- Release tag: `v5.104.2`
- Release message: fix(site-stats): read GA4 with the ops OAuth and carry the pre-switch baseline
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.104.2`.
