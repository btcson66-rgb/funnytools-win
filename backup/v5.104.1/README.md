# v5.104.1 pre-release backup

Created by `npm run release` before publishing v5.104.1.

- Pre-release commit: `7e5a3ae`
- Backup tag: `backup/pre-v5.104.1`
- Release tag: `v5.104.1`
- Release message: fix(analytics): publish gtag on window so custom GA4 events stop being silently dropped
- package-lock version updated: yes

No source archive is stored in git; the backup tag is the rollback snapshot.
To restore the exact pre-release commit, use `git checkout backup/pre-v5.104.1`.
